# CLAUDE.md

This repo's baseline agent context lives in `.github/copilot-instructions.md`
(shared with Copilot so it doesn't drift into two versions). Read it at the
start of any session touching this repo — it covers repo layout, infra
conventions, the Resend email migration, GitHub Actions OIDC gotchas, and
branch protection on `main` (PRs required, no direct pushes, even for admins).

@.github/copilot-instructions.md
